<% content_for(:title,"药品信息录入")%>
<%= form_for(@drug) do |f| %>
  <% if @drug.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@drug.errors.count, "error") %> prohibited this drug from being saved:</h2>

      <ul>
      <% @drug.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <legend>药品信息录入</legend>
  <div class="field">
    <%= f.label "药品名称:" %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label "药品价格:" %>
    <%= f.number_field :price %>
  </div>
  <div class="actions">
    <%= f.submit "录入信息",class: "btn btn-success" %>
  </div>
<% end %>
